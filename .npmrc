# Supply-chain cooldown: don't install dependency versions newer than 7 days.
# Most malicious releases are detected and pulled within hours, so this dodges
# the danger window. Requires npm >= 11.10.0; older npm silently ignores it.
# Value is in DAYS.
min-release-age=7
